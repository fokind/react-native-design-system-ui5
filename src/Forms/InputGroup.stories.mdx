import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import InputGroup from './InputGroup';
import { Text } from 'react-native';
import FormInput from './FormInput';

<Meta title="InputGroup" component={InputGroup} />

# InputGroup

```js
import { InputGroup } from 'react-native-fundmental';
```

A **FormLabel** is used to identify form components such as **FormInput**, **Checkbox** and **TextArea**. Best practice is to use this component as a child of **FormGroup**.

# Examples

## Primary

<Preview>
  <Story name="primary">
    <InputGroup>
      <Text>$</Text>
      <FormInput placeholder="Type text here" />
    </InputGroup>
  </Story>
</Preview>

## Single

<Preview>
  <Story name="single">
    <InputGroup>
      <FormInput placeholder="Type text here" />
    </InputGroup>
  </Story>
</Preview>

# Properties

<Props of={InputGroup} />
