import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import Counter from '../Counter/Counter';
import Inline from './Inline';
import Stack from './Stack';

<Meta title="Inline" component={Inline} />

# Inline
Inline is a layout container which alligns it's children horizontally (inline) and provide constant margin around them.

> Use of Inline is discraged. Use `Stack` with direction="horizontal" instead.

# Usage
```js
import { Box, Counter } from 'react-native-design-system';

<Inline space="xxlarge">
  <Counter onPress={() => this.handlePress(id)}>
    10
  </Counter>
  <Counter onPress={() => this.handlePress(id)}>
    50
  </Counter>
  <Counter onPress={() => this.handlePress(id)}>
    677
  </Counter>
  <Counter onPress={() => this.handlePress(id)}>
    9999
  </Counter>
</Inline>
```

# Props
<Props of={Inline} />

# Space
Customize spacing of each child from one another. `space` prop accepts one of the given values: `none`, `xxsmall`, `xsmall`, `small`, `medium`, `large`, `xlarge`, `xxlarge` and provide constant spacing between each children.

<Preview>
  <Story name="space">
    <Stack>
      <Inline space="none">
        <Counter onPress={action('pressed')}>
          10
        </Counter>
        <Counter onPress={action('pressed')}>
          50
        </Counter>
        <Counter onPress={action('pressed')}>
          677
        </Counter>
        <Counter onPress={action('pressed')}>
          9999
        </Counter>
      </Inline>
      <Inline space="medium">
        <Counter onPress={action('pressed')}>
          10
        </Counter>
        <Counter onPress={action('pressed')}>
          50
        </Counter>
        <Counter onPress={action('pressed')}>
          677
        </Counter>
        <Counter onPress={action('pressed')}>
          9999
        </Counter>
      </Inline>
      <Inline space="xxlarge">
        <Counter onPress={action('pressed')}>
          10
        </Counter>
        <Counter onPress={action('pressed')}>
          50
        </Counter>
        <Counter onPress={action('pressed')}>
          677
        </Counter>
        <Counter onPress={action('pressed')}>
          9999
        </Counter>
      </Inline>
    </Stack>
  </Story>
</Preview>

# Playground
Play with Inline in Canvas mode.

<Story name="playground" parameters={{ decorators: [withKnobs] }}>
 <Inline
  space={select('space', ['none','xxsmall', 'xsmall', 'small', 'medium', 'large', 'xlarge', 'xxlarge'], 'medium')}>
    <Counter onPress={action('pressed')}>
      10
    </Counter>
    <Counter onPress={action('pressed')}>
      50
    </Counter>
    <Counter onPress={action('pressed')}>
      677
    </Counter>
    <Counter onPress={action('pressed')}>
      9999
    </Counter> 
  </Inline>
</Story>